services:
  - type: web
    name: Chosmochords
    env: static
    buildCommand: "npm install --force @vanilla-extract/css@latest @vanilla-extract/vite-plugin@latest vite@latest && npm install && npm run build"
    staticPublishPath: dist
